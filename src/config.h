#pragma once

// Approximate size of all consumed memory.
#define GLOBAL_STACK_ALLOC_SIZE (4 * 1024 * 1024)

// Size of a signle block in request pool in bytes.
#define REQUEST_POOL_BLOCKS_SIZE (1024)

// Count of memory blocks in request pool.
#define REQUEST_POOL_BLOCKS_COUNT 1
